package core.model.party;

import core.model.product.ProductPrototype;

public class Retailer extends  Party{
    public Retailer(Long id, String firstName, String lastName) {
        super(id, firstName, lastName);
    }
}
