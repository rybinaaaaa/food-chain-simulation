package core.model.product.state;

import core.model.product.ProductPrototype;

public class Received extends  ProductState{
    public Received(ProductPrototype product) {
        super(product);
    }
}
